import React, { Component } from 'react';
import { Text, View } from 'react-native';
import styles from '../Config/styles'

export default class raceTime extends Component {
  render() {
    return (
      <View style={styles.container}>
        <Text>Hello, raceTime!</Text>
      </View>
    );
  }
}
